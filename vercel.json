{
    "builds": [{
        "src": "zoomcouriercompany/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "zoomcouriercompany/wsgi.py"
        }
    ]
}